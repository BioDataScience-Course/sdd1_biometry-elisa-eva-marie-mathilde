---
title: "Rapport sur la biomérie humaine 2018"
author: "Mathilde, Eva, Marie, Elisa"
date: "12/10/2018"
output: 
  html_notebook:
    number_sections: yes
    toc: yes
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
SciViews::R
library(skimr)
library(visdat)
```


```{r importation}
#n_bio <- readr::read_csv("https://docs.google.com/spreadsheets/d/e/2PACX-1vQoVtSWbENWzxbALxD0qyNDqxV4uSYqzLCtJgcNGE7ciT6nkWOjA9b6dMBHaSUY8Nw5f-mSpUEeN-3S/pub?output=csv", locale = locale(decimal_mark = ","))

#write(n_bio_fill, file = "../data/biometry.rds", type = "rds", compress = "xz")

n_bio <- read("../data/biometry.rds")


View(n_bio)
summary(n_bio)


# filter

n_bio_fil <- filter(n_bio, taille != "NA")

vis_dat(n_bio_fil)

# change type

n_bio_fil$genre <- factor(n_bio_fil$genre, levels = c("h", "f"))
n_bio_fil$hainaut <- factor(n_bio_fil$hainaut, levels = c("o", "n"))


```

# Introduction
 
 
 
# But 

 Nous avons effectué differentes mesures sur un nombre de 266 individus vivant dans le Hainaut afin de collecter un nombre de données suffisant pour étudier  le taux d'obesité dans cette région.

# Materiels et méthodes 
 
 

# Résultats

```{r graphique 1}
chart(data = n_bio_fil, ~ factor (0) %fill=% genre) +
  geom_bar(width = 1) +
  coord_polar("y", start = 0) +
  theme_void() +
  scale_fill_viridis_d()
```

```{r graphique 2}
chart(data = n_bio_fil, ~ masse) +
  geom_bar(na.rm=TRUE)
```

```{r graphique 3}
chart(data = n_bio_fil, taille ~ masse | genre) +
  geom_point(na.rm=TRUE)
```

```{r graphique 4}
chart(data = n_bio_fil, masse ~ acti_profession %fill=%acti_profession) +
  geom_violin(show.legend = FALSE)
```

```{r graphique 5}
chart(data = n_bio_fil, ~ age %fill=% genre)+
  geom_histogram(bins = 30)
```

```{r graphique 6}
chart(data = n_bio_fil, tour_taille ~ tour_hanche)+
  geom_point(na.rm = TRUE)
```

```{r graphique 7}
chart(data = n_bio_fil, ~ fastfood)+
  geom_bar(na.rm = TRUE)
```

```{r graphique 8}
chart(data = n_bio_fil, alcool ~ age)+
  geom_point(na.rm = TRUE)
```

```{r graphique 9}
chart(data = n_bio_fil, ~ grignotage %fill=% genre)+
  geom_bar()
```

```{r graphique 10}
chart(data = n_bio_fil, ~ tour_poignet %col=% genre)+
  geom_density()
```


## Test d'hypothèse

* Tableau de contigence
```{r}
(n_bio_genre <- as.table(c(" homme" = 135 , "femme"= 131)))
```

* Nombre total d'individus
```{r}
sum(n_bio_genre)
```
 
 * Test de chi²
 
```{r }
chisq.test(n_bio_genre, p = c(1/2, 1/2), rescale.p = FALSE)
```

## Test de Student

```{r}
t.test(data = n_bio_fil, masse ~ genre,
  alternative = "two.sided", conf.level = 0.95, var.equal = TRUE)
```

## Test de Wilcoxon

## Test de Kruskal-Wallis

# Discussion

# Conclusion



